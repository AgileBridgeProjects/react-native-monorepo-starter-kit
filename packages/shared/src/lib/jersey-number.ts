/**
 * Valid jersey number range, mirrored on the backend in
 * `StarterKit.Core.Users.JerseyNumberConstraints`. Changing this value
 * requires the matching backend constant to change as well.
 */
export const JERSEY_NUMBER_MIN = 0;
export const JERSEY_NUMBER_MAX = 99;
