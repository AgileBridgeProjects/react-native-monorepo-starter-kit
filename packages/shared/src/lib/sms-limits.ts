/**
 * Hard limits for SMS messages, mirrored on the backend in
 * `StarterKit.Core.Notifications.SmsLimits`. Changing this value requires the
 * matching backend constant to change as well.
 */
export const SMS_MAX_MESSAGE_LENGTH = 160;
