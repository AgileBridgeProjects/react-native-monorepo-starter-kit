namespace StarterKit.Auth.Constants;

/// <summary>Authentication scheme names used across <c>StarterKit.Auth</c>.</summary>
public static class AuthSchemes
{
    /// <summary>Supabase (GoTrue) JWT validation scheme — the sole auth provider.</summary>
    public const string Supabase = "Supabase";
}
