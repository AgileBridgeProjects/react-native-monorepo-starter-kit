namespace StarterKit.Data.Teams.Enums;

/// <summary>Fixed age-group bands a team competes in. Stored as a string — no ordering semantics.</summary>
public enum AgeGroup
{
    U10,
    U11,
    U12,
    U13,
    U14,
    U15,
    U16,
    U17,
    U18,
}
