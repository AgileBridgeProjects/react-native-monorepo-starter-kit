/**
 * Responsive breakpoints (in dp) — mirrors Tailwind's default scale.
 * Use with `useWindowDimensions` to drive layout changes at runtime.
 */
export const breakpoints = {
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
  '2xl': 1536,
} as const;
