// Stub for static asset files (PNG, JPG, SVG, etc.) in the Node test environment.
// React Native / Metro would normally resolve these to a numeric asset ID;
// returning 1 satisfies all Image source props without requiring native loaders.
export default 1;
